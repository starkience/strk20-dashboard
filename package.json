{
  "name": "strk20-dashboard",
  "version": "0.0.0",
  "private": true,
  "description": "STRK20 privacy pool dashboard — data + components for Akash's Next.js campaign site",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev:server": "pnpm -F @strk20/server dev",
    "dev:dashboard": "pnpm -F @strk20/dashboard dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
